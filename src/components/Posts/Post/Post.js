import React from 'react';

import useStyles from './styles';

const Post = ({post}) => {
    const classes = useStyles();
    return(
        <h1>
            POST
        </h1>
    );

}
export default Post;